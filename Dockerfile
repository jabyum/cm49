# указываем версию пайтона
FROM python:latest
# указываем что и куда нужно сохранить как проект
COPY . /code
#   ^ что ^ куда
# указываем нашу рабочую зону (где хранятся все файлы для запуска)
WORKDIR /code
# указываем что нужно сделать перед запуском проекта (что нужно запустить)
RUN pip install -r requirements.txt
# говорим докеру как запустить наш проект
CMD ["uvicorn", "main:app", "--reload", "--host=0.0.0.0"]



